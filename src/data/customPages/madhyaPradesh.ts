import { SEOManualContent } from '../locationSEOContent';

/**
 * Custom overrides for specific Madhya Pradesh pages if needed.
 * General pages are now generated programmatically to ensure 100% unique copywriting per city,
 * preventing any search engine duplicate content penalty.
 */
export const MADHYA_PRADESH_OVERRIDES: Record<string, Partial<SEOManualContent>> = {};
